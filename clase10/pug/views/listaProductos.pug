extends layout 

block listaProductos 
    -const arrayProducts = productos
    <header>
        <nav style="border-radius: 16px !important; margin: 10px 0 0 0" class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand disabled" href="#">Navbar</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        <a class="nav-link active" aria-current="page" href="/">Formulario</a>
                        <a class="nav-link" href="/productos">Lista</a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    //- <main style="display: flex; justify-content: center; align-items: center;">
    //-     <section style="text-align: center;">
    //-     <h2>Lista Producto</h2>
    //-     <table class="table">
    //-         <thead>
    //-             <tr>
    //-                 <th scope="col">Nombre</th>
    //-                 <th scope="col">Precio</th>
    //-                 <th scope="col">Imagen</th>
    //-             </tr>
    //-         </thead>

    //-         each prod in productos
    //-             <tbody style="line-height: 50px;">
    //-                 <td>prod.name</td>
    //-                 <td>$ prod.precio</td>
    //-                 <td><img style="width: 20%;" src=prod.urlImg alt=""></td>
    //-             </tbody>
    //-     </table>
    //-     //- {{/if}}
    //-     //- {{#else}}
    //-     //-     <h4>No hay productos en la lista</h4>
    //-     //- {{/else}}
    //-     </section>
    //- </main>

    <main style="display: flex; justify-content: center; align-items: center;">
        <section style="text-align: center;">
        <h2>Lista Producto</h2>
        #arrayProducts
            if arrayProducts.length>0
                table.table
                    thead
                        tr
                        th(scope='col') Nombre
                        th(scope='col') Precio
                        th(scope='col') Imagen
                
                    each prod in arrayProducts
                        tbody(style='line-height: 50px;')
                            td 
                                != prod.name
                            td 
                                != prod.precio
                            td  
                                img(src= prod.urlImg , alt="", style="width:20%")
            else
                h4="No hay productos"

    </main>